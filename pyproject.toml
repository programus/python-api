[project]
name = "python-api"
version = "0.1.0"
description = "FastAPI-based Python code execution platform"
requires-python = ">=3.12"
dependencies = [
    "fastapi==0.109.1",
    "uvicorn[standard]==0.24.0",
    "pydantic==2.5.0",
]
